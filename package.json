{
  "name": "discord-relationship-bot",
  "version": "1.0.0",
  "description": "A Discord bot built on OpenClaw that develops a genuine relationship with its owner through natural conversation",
  "main": "index.ts",
  "scripts": {
    "start": "bash start-local.sh",
    "setup": "bash setup-local.sh",
    "reset": "bash reset.sh",
    "fresh": "bash reset.sh && bash setup-local.sh && bash start-local.sh",
    "test": "jest"
  },
  "dependencies": {
    "openclaw": "latest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
